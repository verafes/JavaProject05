public class IsPositiveNumber {

    /**
     * Algorithm IsPositiveNumber
     * checks the number is greater than 0 or not
     * returns true if int number >= 0 else false
     */
    public static boolean isPositiveNumber(int number) {

            return (number >= 0);
    }

}
